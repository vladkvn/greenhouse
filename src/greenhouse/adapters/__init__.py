"""Адаптеры: реализации интерфейсов ядра для конкретных сред (sim, ROS 2, железо).

Ядро (`greenhouse.domain`, `.navigation`, ...) о пакете adapters не знает —
зависимость направлена только внутрь.
"""
